Плейлисты не сохраняются между сессиями — после выхода и повторного входа профиль пустой.

Проблема: генерация и сохранение плейлистов работают через mock API (in-memory), а не через реальную БД.

Нужно:
1. При генерации плейлиста сохранять его в PostgreSQL (таблицы playlists и tracks), привязывая к текущему пользователю по user_id из JWT-токена
2. При загрузке профиля — загружать плейлисты пользователя из PostgreSQL через PlaylistRepository
3. Фронтенд должен работать как раньше — без изменений в UI

Используй уже реализованный PlaylistRepository. Mock API для генерации треков пока оставь (список треков генерируется рандомно), но результат должен сохраняться в БД.